#!/bin/bash

export TSURU_HOST=`config-get tsuru-host`
export TSURU_APPNAME=`config-get tsuru-appname`

juju-log "starting nginx"
service nginx start

juju-log "starting circus"
circusd /etc/circus/circus.ini --daemon
